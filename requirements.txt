# The original crash (TypeError: TyperArgument.make_metavar() takes 1
# positional argument but 2 were given) was caused by a Click 8.2+ update
# breaking typer==0.12.3's internal Click integration -- a known upstream
# incompatibility, not a bug in this project's code.
#
# Fix: typer>=0.26 vendors Click internally, so it no longer depends on
# (and can no longer clash with) a separately-installed click version.
typer==0.26.8

rich>=13.7.1,<14
openai>=1.14.3,<2
pydantic-settings>=2.2.1,<3
python-dotenv>=1.0.1,<2
